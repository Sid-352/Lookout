name: Manual Tweet Fetch
on:
  workflow_dispatch:
    inputs:
      twitter_handle:
        description: 'Twitter handle to check (without the @)'
        required: true
        default: 'github'
jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-browsers-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-browsers-

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Run Lookout for Manual Input
        run: node scraper.js ${{ github.event.inputs.twitter_handle }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      - name: Commit and Push Log Files
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add log/
          git diff --staged --quiet || (git commit -m "Update tweet logs" && git push)

